{
  "name": "ui-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "npm run db:reset:test && playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "db:reset:test": "psql \"postgres://user:pass@host:5432/mi_app_test\" -f scripts/reset-db.sql"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}