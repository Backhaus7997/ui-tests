{
  "name": "ui-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "db:reset:test": "psql \"postgres://user:pass@host:5432/mi_app_test\" -f scripts/reset-db.sql",
    "demo": "PWDEBUG=1 playwright test --ui tests/login-demo.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}